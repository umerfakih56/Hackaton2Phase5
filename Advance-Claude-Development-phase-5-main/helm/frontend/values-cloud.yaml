replicaCount: 2

image:
  repository: your-registry.azurecr.io/todo-frontend
  tag: latest
  pullPolicy: Always

service:
  port: 3000

env:
  NEXT_PUBLIC_BACKEND_URL: "http://backend-backend:8000"

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 256Mi
